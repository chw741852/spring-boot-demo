# bootstrap.yml 通常用于配置不可变参数
spring:
  application:
    name: foo
  cloud:
    config:
      label: master
      uri: http://user:password@localhost:8888
#  profiles:
#    active: development,info,foo
